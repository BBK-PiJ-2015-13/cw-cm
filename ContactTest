import org.junit.*;
import static org.junit.Assert.*;
public class ContactTest {
@Test
public void testsGetId() {
Contact c = new Contact(1, "Steve", "discussing the X project.");
int output = c.getId();
int expected = 1;
assertEquals(output, expected);
}
}